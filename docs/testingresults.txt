LOW when first enter message on offering to handle, where it shows
number of seconds since this happened, starts with just showing a colon.
At refresh, shows "6 seconds ago" or whatever.
LOW after cancelling or completion, take them straight to judge.jsp if
they click the button.
LOW Allow multiple people to set the same email.
